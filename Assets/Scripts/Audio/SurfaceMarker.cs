using UnityEngine;

/// <summary>Indica il suono di una superficie sotto i piedi, per i raycast dei passi o la lettura diretta da LinePath.</summary>
public class SurfaceMarker : MonoBehaviour {
  public SurfaceType surfaceType = SurfaceType.Default;
}
